# Orquestador de Scrapers

Este proyecto alojará un servicio central encargado de programar y ejecutar los tres scrapers existentes (`clv`, `clrir`, `rir1`). Aquí documentaremos la configuración del scheduler, las dependencias y la futura integración remota con el panel de Streamlit.
